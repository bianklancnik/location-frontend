export type LogInData = {
  email: string;
  password: string;
};

export type RegisterData = {
  email: string;
  firstName: string;
  lastName: string;
  password: string;
  confirmPassword: string;
};

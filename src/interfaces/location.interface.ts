export type AddLocationData = {
  address: string;
  lat: number;
  lon: number;
  img: string;
};

export type LocationType = {
  id: number;
  address: string;
  img: string;
  lat: number;
  lon: number;
};
